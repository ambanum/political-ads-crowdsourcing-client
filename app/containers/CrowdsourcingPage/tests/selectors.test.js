// import { selectCrowdsourcingPageDomain } from '../selectors';

describe('selectCrowdsourcingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
